Build me a single-page web app called "DueNext AI" that extracts due dates from course syllabi and exports them to Google Calendar.

Here's the context: Students waste time manually entering syllabus due dates into their calendars. This tool uses AI to automatically extract that information and lets them export it directly to Google Calendar.

It should do these things:

A file upload area where users can upload a PDF syllabus OR paste syllabus text directly
When they upload/paste, send the content to OpenAI API to extract due dates, assignment names, and any grading weights
Display the extracted information in a clean list format showing: Assignment Name, Due Date, Weight (if found)
Sort the assignments chronologically by due date
An "Export to Google Calendar" button that creates calendar events for each assignment
Do NOT include any of the following:

User accounts, login, or authentication
Grade tracking or "cooked meter" features
Multiple course management
Beautiful or polished design — functional is fine
Complex Google Calendar authentication (use a simple calendar link format if needed)
Technical notes:

Build this as a Next.js app
Use OpenAI API for the text extraction
Make it work on mobile
Keep the UI dead simple — one page, upload area, results display, export button
For PDF parsing, use a library like pdf-parse or just focus on text input for now
For Google Calendar export, generate .ics file download or use Google Calendar URL format
The goal is a working version I can show to a college student today and ask "would you use this?" — not a finished product.

Now paste that and build it.

hmm.. i wanna build it on replit
Progress: 45% [█████████░░░░░░░░░░░]

◆ AI+Friends Last ✅ Scope the toy version Now 🟨 Build it with AI Next ⬜ Ship it live

Perfect. Replit makes this super easy.

Go to replit.com, create a new Repl, choose "Node.js" or "Next.js" template, and paste that exact prompt into Replit Agent. It'll build the whole thing for you.

The nice thing about Replit is once it's built, you get a live URL automatically — no separate deployment step.

Go build it and come back with a screenshot when you have something working.

